# Loaded only when Vite runs with --mode test (the verify scripts).
# Forces the demo provider so tests never touch a real database, regardless
# of what is in .env. Vite gives .env.test precedence over .env.
VITE_USE_MOCK=true
